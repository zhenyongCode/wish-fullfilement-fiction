package service

import "context"

func ServiceFuncExe(ctx context.Context, funcName string, params map[string]interface{}) (interface{}, error) {
	// TODO：根据 funcName 调用对应的服务函数，并传入 params 作为参数
	return nil, nil
}
